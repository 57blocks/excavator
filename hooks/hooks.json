{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/post-tool-use-auto-update.mjs\""
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/session-start-freshness-check.mjs\" && echo \"[excavator] Knowledge graph may be stale (source has changed since the last analysis). Read the file at ${CLAUDE_PLUGIN_ROOT}/hooks/auto-update-prompt.md, follow its instructions to detect what structurally changed, and propose running /excavator to the user with a summary of the detected changes. Wait for the user's decision before updating the graph.\" || true"
          }
        ]
      }
    ]
  }
}
